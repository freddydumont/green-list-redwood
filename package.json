{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web",
      "packages/*"
    ]
  },
  "scripts": {
    "extract": "yarn workspace web run extract"
  },
  "devDependencies": {
    "@redwoodjs/core": "^0.6.0",
    "chalk": "^4.0.0"
  },
  "eslintConfig": {
    "extends": "@redwoodjs/eslint-config"
  },
  "engines": {
    "node": ">=12",
    "yarn": ">=1.15"
  }
}
