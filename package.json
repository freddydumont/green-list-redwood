{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web"
    ]
  },
  "scripts": {
    "extract": "i18next --config i18next-parser.config.js"
  },
  "devDependencies": {
    "@redwoodjs/core": "^0.4.0",
    "chalk": "^4.0.0"
  },
  "eslintConfig": {
    "extends": "@redwoodjs/eslint-config"
  },
  "engines": {
    "node": ">=12",
    "yarn": ">=1.15"
  }
}
