{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web"
    ]
  },
  "scripts": {
    "extract": "yarn workspace web run extract"
  },
  "devDependencies": {
    "@redwoodjs/core": "^0.5.0",
    "chalk": "^4.0.0"
  },
  "eslintConfig": {
    "extends": "@redwoodjs/eslint-config"
  },
  "engines": {
    "node": ">=12",
    "yarn": ">=1.15"
  }
}
